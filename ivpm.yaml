package:
    name: fwvip-common

    dep-sets:
    - name: default
      deps: []

    - name: default-dev
      deps: []

    paths:
      export:
        lib-dirs:
          - src
      project:
        lib-dirs:
          - src
