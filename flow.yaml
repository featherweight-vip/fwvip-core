
package:
  name: org.featherweight-vip.fwvip-common

  imports:
  - "${{ rootdir }}/packages/fwprotocol-defs/flow.yaml"

  tasks:
  - name: src-hdl
    uses: std.FileSet
    with:
      type: verilogIncDir
      base: src/vip

  - name: src-hvl
    uses: std.FileSet
    needs: [src-hdl]
    with:
      type: systemVerilogSource
      base: src/vip
      include: "*.sv"
      incdirs: ["."]
  
  fragments:
  - src/verif/verif.yaml

